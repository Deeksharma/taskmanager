# task-management-service

For Development environment, change env variables in `configs/configs.yml` and `docker.env.dev` according to you, then
run this command

```makefile
make run_dev
```

To kill development server,

```makefile
make kill_dev
```

To check linting, run

```makefile
make lint
```
